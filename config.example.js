// 설정 템플릿 — 이 파일을 복사해서 같은 폴더에 config.js 로 저장한 뒤 값을 채우세요.
// (config.js 는 .gitignore에 등록되어 GitHub에 올라가지 않습니다.)
//
// Supabase 값 위치: 대시보드 → Project Settings → API
//   - url      : Project URL
//   - anonKey  : Project API keys 의 "anon public" 키
window.SUPABASE_CONFIG = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY"
};
